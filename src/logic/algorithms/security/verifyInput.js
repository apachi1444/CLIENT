// import { isEmail } from 'validator';

// export function verifyInput(label, value){
//     const passRegex=/^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]{8,}$/;
//     switch(label.toLowerCase()){
//         case 'email':
//             if(isEmail(value)) return false
//             return 'Incorrect email, please provide a valid one!';
//         case 'password':
//             if(passRegex.test(value)) return false;
//             return 'Minimum eight characters, at least one letter and one number';
//         default:
//             return false;

//     };
// }
